@using Syring1.Areas.Admin.ViewModels.LastestNew
@model LastestNewIndexViewModel



<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">News</h4>
                        <a class="btn btn-primary" asp-action="create" asp-controller="lastestnew" asp-area="admin">CREATE lastestnew</a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Topic</th>
                                        <th>DateTime</th>
                                        <th>Photo</th>
                                        <th>Actions</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var lastestNew in Model.LastestNews)
                                    {
                                        <tr>
                                            <td>@lastestNew.Id</td>
                                            <td>@lastestNew.Title</td>
                                            <td>@lastestNew.Topic</td>
                                            <td>@lastestNew.CreatedDate</td>
                                            <td><img src="~/assets/img/@lastestNew.PhotoPath" /></td>
                                            <td>
                                                <a class="btn btn-primary" asp-action="update" asp-controller="lastestnew" asp-area="admin" asp-route-id="@lastestNew.Id">Update</a>
                                                <a class="btn btn-danger" onclick="confirm('Eminsinizmi?')" asp-action="delete" asp-controller="lastestnew" asp-area="admin" asp-route-id="@lastestNew.Id">Delete</a>
                                                <a class="btn btn-info" asp-action="details" asp-controller="lastestnew" asp-area="admin" asp-route-id="@lastestNew.Id">Details</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


